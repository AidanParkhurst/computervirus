{
  "name": "computervirus",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env dev",
    "deploy": "wrangler deploy --env production"
  },
  "devDependencies": {
    "wrangler": "^4.121.0"
  }
}
